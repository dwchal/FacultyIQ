# FacultyIQ Python - Environment Variables
# Copy this file to .env and update with your values

# OpenAlex API (optional but recommended for higher rate limits)
# Register at https://openalex.org/
OPENALEX_EMAIL=your.email@institution.edu

# Scopus API (optional)
# Get API key from https://dev.elsevier.com/
SCOPUS_API_KEY=your_scopus_api_key

# Google Scholar rate limiting (optional)
# Increase delay if getting blocked
SCHOLAR_RATE_LIMIT=3.0
